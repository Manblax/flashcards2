# FlashCards2 - Обзор проекта

## 📋 Описание

FlashCards2 - современное веб-приложение для изучения материалов с помощью флэш-карточек. Разработано с использованием Next.js 14, TypeScript и DaisyUI.

## ✨ Реализованные функции

### 1. Главная страница (/)
- ✅ Отображение 6 последних модулей
- ✅ Карточки модулей с информацией о количестве терминов и авторе
- ✅ Адаптивная сетка (2 колонки на десктопе)
- ✅ Hover эффекты и плавные переходы

### 2. Страница библиотеки (/library)
- ✅ Табы для навигации (текущий таб: "Модули")
- ✅ Бесконечная прокрутка (Infinite Scroll)
- ✅ Загрузка по 20 модулей за раз
- ✅ Группировка модулей по временным периодам:
  - "НА ЭТОЙ НЕДЕЛЕ"
  - "НА ПРОШЛОЙ НЕДЕЛЕ"
  - По месяцам (например, "МАРТ 2025 Г.")
- ✅ Индикатор загрузки
- ✅ Сообщение о завершении списка

### 3. Layout компоненты
- ✅ **Sidebar** с:
  - Логотипом приложения
  - Меню навигации (Главная, Ваша библиотека)
  - Подсветка активного пункта
  - Иконки для каждого раздела
  
- ✅ **Header** с:
  - Меню-гамбургер
  - Поле поиска
  - Кнопка создания модуля (круглая с иконкой +)
  - Аватар пользователя
  - Sticky позиционирование

### 4. Дополнительные страницы
- ✅ Страница создания модуля (/create) - базовая форма
- ✅ Страница просмотра модуля (/module/[id]) - динамический роутинг

## 🎨 Дизайн

### Цветовая палитра
```
Primary:    #6366f1 (индиго)
Base-100:   #0a0e27 (темно-синий фон)
Base-200:   #141937 (sidebar фон)
Base-300:   #1e2549 (карточки)
Neutral:    #2a3256 (границы)
Text:       #a6adbb (вторичный текст)
```

### Особенности UI
- Темная тема по умолчанию
- Скругленные углы (rounded-2xl для карточек)
- Мягкие тени и границы
- Плавные hover эффекты
- Адаптивный дизайн

## 🏗️ Архитектура

### Технологический стек
```
Frontend:
├── Next.js 14 (App Router)
├── React 18
├── TypeScript 5
├── Tailwind CSS 3
└── DaisyUI 4

Инструменты:
├── PostCSS
├── Autoprefixer
└── ESLint
```

### Структура каталогов
```
flashcards2/
├── app/                        # Next.js App Router
│   ├── layout.tsx             # Корневой layout с MainLayout
│   ├── page.tsx               # Главная страница
│   ├── globals.css            # Глобальные стили
│   ├── library/
│   │   └── page.tsx           # Страница библиотеки
│   ├── create/
│   │   └── page.tsx           # Страница создания модуля
│   └── module/[id]/
│       └── page.tsx           # Динамическая страница модуля
│
├── components/                 # React компоненты
│   ├── layout/
│   │   ├── MainLayout.tsx     # Основной layout
│   │   ├── Header.tsx         # Шапка приложения
│   │   └── Sidebar.tsx        # Боковая панель
│   ├── ModuleCard.tsx         # Карточка модуля
│   └── InfiniteModuleList.tsx # Список с бесконечной прокруткой
│
├── types/                      # TypeScript типы
│   └── module.ts              # Интерфейсы для модулей
│
├── lib/                        # Утилиты и данные
│   └── mockData.ts            # Генератор моковых данных
│
└── public/                     # Статические файлы
```

## 🔧 Технические детали

### 1. Бесконечная прокрутка (Infinite Scroll)
**Реализация:** `components/InfiniteModuleList.tsx`

```typescript
- Использует Intersection Observer API
- Загружает 20 модулей за раз
- Триггер загрузки - элемент в конце списка
- Защита от двойной загрузки (isLoading флаг)
- Определение конца списка (hasMore флаг)
```

### 2. Группировка модулей по времени
**Логика:**
```typescript
const groupModulesByPeriod = (modules: Module[]) => {
  - НА ЭТОЙ НЕДЕЛЕ (последние 7 дней)
  - НА ПРОШЛОЙ НЕДЕЛЕ (8-14 дней назад)
  - По месяцам (старше 14 дней)
}
```

### 3. Правильное склонение слов
**Функция:** `getTermWord(count: number)`
```typescript
1 термин
2-4 термина
5+ терминов
11-19 терминов (исключение)
```

### 4. Динамический роутинг
```
/module/[id] - динамическая страница для каждого модуля
Параметр id доступен через params
```

## 📊 Данные

### Структура модуля
```typescript
interface Module {
  id: string;           // Уникальный идентификатор
  title: string;        // Название модуля
  termCount: number;    // Количество терминов
  author: string;       // Автор модуля
  createdAt: Date;      // Дата создания
  updatedAt: Date;      // Дата последнего обновления
}
```

### Моковые данные
```typescript
- 100 модулей
- Разнообразные названия (Lesson 440, Lesson 363-366, и т.д.)
- Количество терминов: от 8 до 31
- Автор: "manblax"
- Даты: последние ~200 дней
```

## 🚀 Производительность

### Оптимизации
- ✅ Server Components по умолчанию (Next.js 14)
- ✅ Client Components только где необходимо
- ✅ Ленивая загрузка модулей (Infinite Scroll)
- ✅ Оптимизированные изображения через next/image (если будут)
- ✅ Минимальный JavaScript на клиенте

### Рекомендации для продакшена
```bash
# Сборка для продакшена
npm run build

# Анализ бандла
npm run build -- --analyze
```

## 📱 Адаптивность

### Breakpoints (Tailwind)
```
sm:  640px
md:  768px
lg:  1024px
xl:  1280px
2xl: 1536px
```

### Адаптивные компоненты
- Sidebar: скрытие на мобильных (будущая реализация)
- Карточки: 1-2 колонки в зависимости от экрана
- Header: адаптивное расположение элементов

## 🔮 Планы развития

### Краткосрочные (MVP)
- [ ] Полноценная форма создания модулей
- [ ] CRUD операции для модулей
- [ ] Добавление/редактирование терминов
- [ ] Страница изучения карточек

### Среднесрочные
- [ ] Система аутентификации (NextAuth.js)
- [ ] База данных (PostgreSQL + Prisma)
- [ ] API роуты для работы с данными
- [ ] Функциональный поиск
- [ ] Фильтры и сортировка

### Долгосрочные
- [ ] Статистика обучения
- [ ] Система повторений (Spaced Repetition)
- [ ] Экспорт/импорт модулей
- [ ] Совместное использование модулей
- [ ] Мобильное приложение (React Native)

## 📚 Документация

### Доступные документы
1. **README.md** - общая информация о проекте
2. **QUICK_START.md** - быстрый старт за 3 шага
3. **COMPONENTS.md** - подробная документация компонентов
4. **PROJECT_OVERVIEW.md** (этот файл) - общий обзор

### Комментарии в коде
Код хорошо прокомментирован для лучшего понимания:
- Назначение каждого компонента
- Сложные функции и логика
- Props интерфейсы
- Важные технические решения

## 🧪 Тестирование (будущее)

### План тестирования
```
Unit тесты:
- Утилиты (склонение слов, группировка)
- Чистые функции

Integration тесты:
- Компоненты с данными
- Навигация между страницами

E2E тесты:
- Создание модуля
- Изучение карточек
- Бесконечная прокрутка
```

## 🤝 Вклад в проект

### Принципы разработки
- **SOLID** - следование принципам объектно-ориентированного программирования
- **KISS** - простота и понятность кода
- **DRY** - избегание дублирования
- **Clean Code** - читаемый и поддерживаемый код

### Стиль кода
- TypeScript strict mode
- ESLint для проверки качества
- Функциональный подход в React
- Hooks вместо классов

## 📄 Лицензия

MIT License - свободное использование и модификация

---

**Дата создания:** Декабрь 2025  
**Версия:** 0.1.0  
**Статус:** MVP готов ✅

